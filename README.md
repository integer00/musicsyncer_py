# musicsyncer_py
python client for mysicsyncer - https://github.com/integer00/musicsyncer

####USAGE:

>./app config [test,prod]
- creates config file in ~/.config folder for given name, if empty makes an test config

>./app upload ['item', [...]]
- upload file or set of files to server

>./app sync ['folder']
- sync local folder with remote
